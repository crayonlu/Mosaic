// Re-export types from @mosaic/api shared package
export type { ApiError, HealthResponse, PaginatedResponse } from '@mosaic/api'
// Note: ServerConfig is now exported from settings.ts as DesktopServerConfig
